<section class="section">
  <div class="section-heading">
    <span class="eyebrow">{{ shop.name }}</span>
    <h1>{{ page.title }}</h1>
  </div>
  <div class="glass-card" style="padding:32px;line-height:1.8;color:var(--muted)">
    {{ page.content }}
  </div>
</section>
{% schema %}
{"name":"Main page","settings":[]}
{% endschema %}
