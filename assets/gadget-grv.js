document.addEventListener('DOMContentLoaded',()=>{const overlay=document.querySelector('[data-cart-drawer]');document.querySelectorAll('[data-open-cart]').forEach((button)=>button.addEventListener('click',()=>overlay&&overlay.classList.add('open')));document.querySelectorAll('[data-close-cart]').forEach((button)=>button.addEventListener('click',()=>overlay&&overlay.classList.remove('open')));document.querySelectorAll('[data-quick-add]').forEach((form)=>{form.addEventListener('submit',async(event)=>{event.preventDefault();const formData=new FormData(form);try{await fetch('/cart/add.js',{method:'POST',body:formData,headers:{Accept:'application/json'}});const overlay=document.querySelector('[data-cart-drawer]');if(overlay)overlay.classList.add('open');}catch(error){form.submit();}});});});
